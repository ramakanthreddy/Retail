package com.javahome.app.assemblers;


import com.javahome.dao.entity.UserEntity;
import com.javahome.web.vo.UserVO;


public class UserAssembler {
	
	
	/*public static void fromUserEntity(UserEntity userEntity)
	{
		UserVO userVO = new UserVO();
		if(userEntity!=null)
		{
			userVO.setUsersid(userEntity.getUsersid());
			//userVO.setUsersname(userEntity.getUsersname());
			userVO.setPasssword(userEntity.getPasssword());
			userVO.setMobile(userEntity.getMobile());
			userVO.setEnable_flag(userEntity.getEnable_flag());
			userVO.setRoleid(userEntity.getRoleid());
		}
	}
*/
}
